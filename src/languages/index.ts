import * as VnMessage from './vn';

const Attributes = VnMessage.Attributes;
const ErrorMessage = VnMessage.ErrorMessage;
const ValidationMessage = VnMessage.ValidationMessage;

export { Attributes, ErrorMessage, ValidationMessage };
